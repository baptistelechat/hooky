{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "menu",
  "description": "Capability for the pet context menu window",
  "windows": ["menu"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-set-focus"
  ]
}
