---
id: BLK-037
type: blocker
date: 2026-09-24
tags: [react-doctor, effect-needs-cleanup, false-positive, debugging, useeffect]
---

# BLK-037 — effect-needs-cleanup persistant après 2 restructurations

| Friction | Cause réelle | Solution | Statut |
| --- | --- | --- | --- |
| React Doctor signalait `effect-needs-cleanup` sur `useCursorGaze` (score 53) alors que le `setTimeout` était bien nettoyé ; deux restructurations (cleanup toujours retourné, `setInterval` dans une expression conditionnelle) ne changeaient rien. | Le détecteur descend dans les fonctions imbriquées (`setTimeout` récursif dans `tick` async) mais cherche le nettoyage au premier niveau de l'effet ; un `setInterval` dans une expression ternaire n'était pas reconnu non plus. Ce n'était pas un vrai leak. | Lecture de la doc canonique de la règle (`prompts/rules/...`) : garde en amont, `setInterval` posé directement dans le corps de l'effet, `clearInterval` retourné, garde `busy` contre le chevauchement. Score 53 → 60, plus aucun problème. | résolu |

## Références

- GLRN-308 (mémoire globale) — pattern extrait
